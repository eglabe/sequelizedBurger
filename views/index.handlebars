<h1>Eat Da Burger!</h1>

<h2>Menu</h2>

<ul>
  {{#each burgers}}
    {{#unless this.devoured}}
      <li class= "browse-submit-inline">
        {{this.burger_name}}

        <form class= "browse-submit-inline" action="/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden" name="devoured" value="true">
          <button class="btn btn-success btn-xs" type="submit">Devour!</button>
        </form>
      </li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Eaten</h2>

<ul>
  {{#each burgers}}
    {{#if this.devoured}}
      <li>
        {{this.burger_name}}

      </li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-update-form" action="/" method="POST">

  <div class="form-group">
    <label for="burg">Burger Name:</label>
    <input type="text" id="burg" name="burgerName">
  </div>

  <button class="btn btn-warning" type="submit">Add Burger</button>
</form>